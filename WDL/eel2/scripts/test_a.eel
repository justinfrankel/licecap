sum=0;
a= 1;
b = .1;
c= .2;
d = .5;
e = 4;
f = -0.2;
g = 0.3;

loop(62000,
  i = 0;
  sum=0.0;
  loop(8192,
    sum += i*(3+ a * b + c*d + e * (f - b*(-c)  + g*(e-g*(-g)*g)) );
    i += 0.001;
  );
  );
printf("sum %f  (%s)\n",sum,sum==245333.476966 ? "OK " : " FAIL");
